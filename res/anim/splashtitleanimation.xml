<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android" android:shareInterpolator="false">
    <scale
        android:fromXScale="0.0"
        android:toXScale="1.0"
        android:fromYScale="0.0"
        android:toYScale="1.0"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="4000"/>
	<!-- cycles: número de repeticiones de la animación. Por defecto 1. -->
	<!-- interpolation: Define la velocidad de cambio de una animación. Añade efectos. Hay ya definidos por Android.  This allows the basic animation effects (alpha, scale, translate, rotate) to be accelerated, decelerated, repeated, etc. -->
    <rotate
    	android:interpolator="@android:anim/cycle_interpolator"
    	android:cycles="4"
        android:fromDegrees="0.0"
        android:toDegrees="360.0"
        android:pivotX="50%"
        android:pivotY="50%"
 		android:duration="4000"/>
</set>
